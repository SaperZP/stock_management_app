import {getSxStyles} from "../../utils/getSxStyles.ts";

const firmsPageStyles = getSxStyles({
  firmsContainer: {
    display: 'flex',
    flexWrap: 'wrap',
    gap: '10px',
    justifyContent: 'center',
  },
});

export default firmsPageStyles;
